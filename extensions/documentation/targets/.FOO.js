'use strict';

function filter(creep, opts) {
    opts = opts || {};

    return {
        filter: function(target) {
            // Insert code
        }
    };
}

// Room === undefined -> get all
function get(room, option) {

}

module.exports = {
    filter: filter,
    get: get
};
